<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Theme applied to the Android window while the process is starting.
         After the Flutter UI initializes, this theme continues to determine
         the window background behind the Flutter UI. -->
    <style name="LaunchTheme" parent="@android:style/Theme.Black.NoTitleBar">
        <item name="android:windowBackground">@android:color/white</item>
    </style>

    <!-- Normal theme for the app once it has started. -->
    <style name="NormalTheme" parent="@android:style/Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@android:color/white</item>
    </style>
</resources>
